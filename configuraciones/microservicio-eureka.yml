server:
  port: 8761

eureka:
  instance:
    hostname: microservicio-eureka.railway.internal
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  metrics:
    binders:
      system:
        enabled: false


# server:
#   port: ${EUREKA_SERVER_PORT:8761}

# spring:
#   application:
#     name: msvc-eureka

# eureka:
#   instance:
#     hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}
#   client:
#     register-with-eureka: false
#     fetch-registry: false
#     service-url:
#       defaultZone: http://${EUREKA_INSTANCE_HOSTNAME:localhost}:${server.port}/eureka/

# management:
#   endpoints:
#     web:
#       exposure:
#         include: health,info,metrics