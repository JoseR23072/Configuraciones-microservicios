server:
  port: 8761

eureka:
  instance:
    hostname: microservicio-eureka.railway.internal
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://microservicio-eureka.railway.internal:8761/eureka/

management:
  metrics:
    binders:
      system:
        enabled: false
